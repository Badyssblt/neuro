meta {
  name: Patch Learning Path
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/learning_paths/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/merge-patch+json
  Accept: application/ld+json
}

body:json {
  {
    "estimated_duration": 5400
  }
}
