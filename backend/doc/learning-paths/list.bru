meta {
  name: List Learning Paths
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/learning_paths
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/ld+json
}
