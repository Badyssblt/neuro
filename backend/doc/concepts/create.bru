meta {
  name: Create Concept
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/concepts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
  Accept: application/ld+json
}

body:json {
  {
    "title": "Les types primitifs",
    "description": "string, number, boolean, null, undefined en TypeScript",
    "difficulty": "beginner",
    "importance_score": 9,
    "estimated_time": 30,
    "module": "/api/modules/1"
  }
}
