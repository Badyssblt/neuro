meta {
  name: Patch Learning Goal
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/learning_goals/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/merge-patch+json
  Accept: application/ld+json
}

body:json {
  {
    "status": "completed"
  }
}
